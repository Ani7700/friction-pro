specVersion: "v1beta"
serviceId: "friction"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "friction-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
